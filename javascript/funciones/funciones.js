function cuboDeUnNumero(numero) {
    let rpta = numero * numero * numero;
    return rpta;
}

let respuesta = cuboDeUnNumero(6);
console.log(respuesta);


/**para ayudar a documentar primero escribo la funcion y luego arriba coloco /** y enter */
/* js doc pagina para documentar en javascript
/**
 * Funcion que recibe un arreglo de nombres tipos string y un nombre a buscar dentro del arreglo. si el nombre existe retorna true y sino false
 * @param {Array<string>} nombres 
 * @param {string} nombreBuscado 
 * @returns {boolean}
 */
function buscarNombre(nombres, nombreBuscado) {

}